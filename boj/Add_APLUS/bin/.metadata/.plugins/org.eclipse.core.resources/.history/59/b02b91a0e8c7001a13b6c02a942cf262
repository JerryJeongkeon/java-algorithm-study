package programmers_Lv2;

import java.util.Arrays;
import java.util.HashMap;
import java.util.Map;

public class 영어끝말잇기 {
	static int n = 5;
	static String[] words = { "tank", "kick", "know", "wheel", "land", "dream", "mother", "robot", "tank" };

	public static void main(String[] args) {
		Map<String, Integer> map = new HashMap<>();
		int[] result = new int[2];
		boolean flag = true;

		map.put(words[0], 1);
		char lastChar = words[0].charAt(words[0].length() - 1);
		char firstChar = '.';
		for (int i = 1; i < words.length; i++) {
			System.out.println(" i : " + i + ", words : " + words[i] + ", last : " + lastChar + ", first : " + firstChar);
			if (!flag) {
				result[0] = i % n + 1;
				result[1] = i / n + 1;
				break;
			}

			firstChar = words[i].charAt(0);
			if (lastChar != firstChar || map.containsKey(words[i])) {
				flag = false;
				continue;
			}

			map.put(words[i], i + 1);
		}

		System.out.println(Arrays.toString(result));
	}
}